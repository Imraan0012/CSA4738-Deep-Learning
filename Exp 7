import numpy as np
import matplotlib.pyplot as plt

def sigmoid(z):
    return 1 / (1 + np.exp(-z))

# Try a wider range
x = np.arange(-10, 10, 0.1)
plt.plot(x, sigmoid(x), color='purple')
plt.title('Sigmoid Function (Range -10 to 10)')
plt.show()
